{
  "name": "fred.site",
  "version": "2.0.0",
  "description": "Personal site",
  "source": "src/pug/index.pug",
  "scripts": {
    "dev": "yarn clear && parcel",
    "build": "yarn clear && parcel build",
    "clear": "rm -rf dist/* && rm -rf .parcel-cache/*"
  },
  "dependencies": {},
  "devDependencies": {
    "@parcel/transformer-less": "2.4.1",
    "@parcel/transformer-pug": "2.4.1",
    "parcel": "2.4.1"
  }
}