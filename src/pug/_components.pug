mixin head (pageTitle)
	head
		meta(charset="utf-8")
		meta(http-equiv="x-ua-compatible" content="ie=edge")

		title= pageTitle

		meta(name="description" content=meta.description)
		meta(name="keywords" content=meta.keywords)

		meta(property="og:type" content="website")
		meta(property="og:url" content=og.url)
		meta(property="og:title" content=meta.title)
		meta(property="og:image" content=og.image)
		meta(property="og:description" content=meta.description)
		meta(property="og:site_name" content=meta.title)
		meta(property="og:locale" content="en_US")
		meta(property="og:image:width" content="1200")
		meta(property="og:image:height" content="630")

		meta(name="twitter:card" content="summary")
		meta(name="twitter:site" content='@' + contacts.twitter)
		meta(name="twitter:creator" content='@' + contacts.twitter)
		meta(name="twitter:url" content=og.url)
		meta(name="twitter:title" content=meta.title)
		meta(name="twitter:description" content=meta.description)
		meta(name="twitter:image" content=og.image)

		meta(name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover")
		meta(name="format-detection" content="telephone=no")

		link(rel="icon" type="image/x-icon" href="/src/assets/favicon.ico")
		link(rel="icon" type="image/png" href="/src/assets/favicon.png")

		link(rel="stylesheet" type="text/css" href="/src/less/style.less")

		block

mixin icon (name)
	i(data-lucide= name).icon

mixin btn (caption, url, preview)
	a(href=url target="_blank" rel="noopener noreferrer").btn
		span.btn--caption= caption
		span.btn--preview= preview

		svg(xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true").icon
			path(d="M7 7h10v10")
			path(d="M7 17 17 7")

mixin experience (company, date, location, summary, tags)
	.experience
		.experience--base
			.experience--company= company
			.experience--date= date + ' · ' + location
		.experience--summary= summary
		.experience--tags
			each tag in tags
				span= '#' + tag.toLowerCase().replace(' ', '_')